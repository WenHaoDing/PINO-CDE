Building.cdb is used as the input.
However, the user must use the Building.inp file to generate the Building.cdb file because the Building.cbd file in this folder is generated in ANSYS afterwards, which is not the input.
We used hypermesh to transfer the inp file into cdb file.